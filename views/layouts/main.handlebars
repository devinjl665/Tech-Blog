<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ask IT - Tech Blog</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"/>
    <link rel="stylesheet" href="/css/style.css"/>
</head>
<body>
    <nav class="navbar navbar-custom">
        <div class="container-fluid">
            <div class="navbar-header">
                <a class="navbar-brand"><h1 style="color:#000000;">Ask IT - Tech Blog</h1></a>
            </div>
            <ul class="nav navbar-nav navbar-center">
                <li><a href="/"><h1>Homepage</h1></a></li>
                <li><a href="/dashboard"><h1>Dashboard</h1></a></li>
                <li>
                {{#if logged_in}}
                    <a><button type="button" class="unButton" id="logout"><h1>Logout</h1></button></a>
                {{else}}
                    <a class="nav-item nav-link" href="/login">Login</a>
                {{/if}}
            </div>
        </div>
    </nav>

    <header class="text-center">        
            <div class="container d-flex align-items-center flex-column">
                <h1 id="header">Ask IT - Tech</h1>
            </div>        
    </header>

    <main class="col-auto">
        {{{body}}}
    </main>
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>
</html>